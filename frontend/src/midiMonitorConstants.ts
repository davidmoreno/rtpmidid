/** Max decoded MIDI events kept in the monitor table (newest at top). */
export const MONITOR_MAX_ROWS = 1024;

/** Highlight duration for newly received rows (milliseconds). */
export const MONITOR_HIGHLIGHT_MS = 2500;
